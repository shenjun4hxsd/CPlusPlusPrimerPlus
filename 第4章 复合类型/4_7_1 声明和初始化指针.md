##声明和初始化指针

```javascript
    // 指针都是基于其他类型的
    int* p;
    int *p1;
    int*p2, p3;  // 声明了一个指针（p1）和一个int变量（p2）对每个指针都需要使用一个 *。
    double *p4;  // *p4保存的是double类型的值 p4是指向double类型的指针
    char* p5;
    // p4和p5虽然指向的两种长度不同的类型，但p4和p5这两个变量本身的长度是相同的。地址需要2个或者4个字节，取决于操作系统。
```

🔚